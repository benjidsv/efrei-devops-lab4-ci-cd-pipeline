version: "3.8"
services:
  backend:
    build:
      context: ./backend
    image: backend:latest
    ports:
      - "3001:3001"
    env_file:
      - .env

  frontend:
    build:
      context: ./frontend
    image: frontend:latest
    ports:
      - "3000:3000"
    env_file:
      - .env
